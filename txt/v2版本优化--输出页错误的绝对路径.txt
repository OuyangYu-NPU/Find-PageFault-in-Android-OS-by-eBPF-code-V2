重写了路径获取逻辑，完全避免使用 bpf_d_path()，改为：

手动遍历 dentry 树：显式地向上遍历最多 5 层目录结构
避免复杂循环：不使用数组和动态循环，而是使用固定的变量（d0, d1, d2, d3, d4）
使用 per-CPU 缓冲区：避免栈溢出问题
逐层构建路径：从根目录到文件名，逐个添加路径组件
关键改进
✅ 无 bpf_d_path() 依赖 - 适用于旧内核
✅ 无复杂循环 - 满足 eBPF 验证器要求
✅ 无栈溢出 - 使用 per-CPU map 存储临时数据
✅ 路径深度限制 - 最多支持 5 层目录（对大多数场景足够）